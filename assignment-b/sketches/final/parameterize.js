let angle = 0;

function setup() {
  createCanvas(windowWidth, windowHeight);
  background(0);

  }

function draw() {

  for(var x = 0; x<2; x++) {
    for (var y = 0; y<1; y++){
      rect(30, 20, 10, 10);
      scale(random(0,100));


}
}
}
